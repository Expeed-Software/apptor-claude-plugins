{
  "name": "apptor-flow",
  "description": "Author Apptor Flow workflows as parser-valid flow JSON from a natural-language description, using the live node schema. Validates through the real JSON parser and can create the flow as a draft via the REST API (a human publishes in the designer). Draft-only, org-scoped.",
  "version": "1.0.1",
  "author": {
    "name": "Expeed Software"
  }
}
